spring:
  application:
    name: order
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    username: root
    password: 123456
    url: jdbc:mysql://127.0.0.1:3306/springCloud?characterEncoding=utf-8&&useSSL=false
  jpa:
    show-sql: true
eureka:
  client:
    service-url:
      defaultZone: http://master:9000/eureka/,http://slave:9001/eureka/
server:
  port: 8002
env:
  test
